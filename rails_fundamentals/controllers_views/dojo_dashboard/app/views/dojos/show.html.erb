<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <title>Dojo</title>
</head>
<body>
    <div class= "container mx-auto"> 
        <h1 class = "text-center mt-5 bg-warning text-white p-3" ><%= @dojo.branch %> Dojo</h1>
        <div class="row">
            <div class= "col text-center bg-light mb-5"> 
                <p>Address: <%= @dojo.street %></p>
                <p class="col">City: <%= @dojo.city %> 
                <span class="col"> State: <%= @dojo.state %></span></p>
                <a href="/dojos/<%= @dojo.id %>/edit" class="btn btn-dark py-0 mb-4"> Edit </a>
            </div> 
        </div>
    </div>    
    <div class= "container mx-auto">
        <div class= "d-flex row bg-info"> 
            <h4 class = "mr-auto text-white p-2">Student Roster</h4>
            <a href="/dojos/<%= @dojo.id %>/students/new" class="text-white p-2">Add a Student </a>
        </div>
        <table class = "table w-auto">
            <thead>
                <tr>
                    <th scope ="col p-3">First Name</th>
                    <th scope ="col">Last Name</th>
                    <th scope ="col">Email</th>
                    <th scope ="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                <% @student.each do |student| %>
                    <tr>
                        <td><%= student.first_name %></td>
                        <td ><%= student.last_name %></td>
                        <td><%= student.email %></td>
                        <td class="row m-0">
                            <a href="/dojos/<%= @dojo.id %>/students/<%= student.id %>" class="text-link mr-1">Show </a>
                            <a href="/dojos/<%= @dojo.id %>/students/<%= student.id %>/edit" class="text-link"> | Edit | </a>
                            <form action= '/dojos/<%= @dojo.id %>/students/<%= student.id %>' method ='POST'>
                                <input type ="hidden" name="_method" value="delete">
                                <input type ="hidden" name ="authenticity_token" value ="<%=form_authenticity_token%>">
                                <input type="submit" value="Delete" class="btn btn-link p-0 ml-1" style="margin-top: -4px;">
                            </form>
                        </td>
                    </tr>
                <% end %>
            </tbody>
        </table>
        <div class="row justify-content-end mr-1 mb-2 ">
            <b><a href="/dojos" class="text-dark col">Go Home </a></b>
        </div>   
    </div>
</body>
</html>


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <title>Dojo</title>
</head>
<body>
    <div class= "container mx-auto"> 
        <h1 class = "text-center mt-5 bg-secondary text-white p-3" ><%= @student.first_name%> <%= @student.last_name%></h1>
        <div class="row">
            <div class= "col text-center bg-light mb-5"> 
                <p>Email: <%= @student.email %></p>
                    <a href="/dojos" class="text-link">Home </a>
                    <a href="/dojos/<%= @dojo.id %>/students/<%=@student.id %>/edit" class="text-link"> | Edit </a>
            </div> 
        </div>
    </div>    
    <div class= "container mx-auto"> 
        <h4 class = "bg-light text-dark p-3"><%= @dojo.branch %> Cohort</h4>
        <table class = "table w-auto">
            <thead>
                <tr>
                    <th scope ="col p-3">First Name</th>
                    <th scope ="col">Last Name</th>
                    <th scope ="col">Email</th>
                </tr>
            </thead>
            <tbody>
                <% @dojo.students.each do |student| %>
                    <% if student.created_at.strftime("%Y-%m-%d") == @student.created_at.strftime("%Y-%m-%d")%>
                        <tr>
                            <td><%= student.first_name %></td>
                            <td ><%= student.last_name %></td>
                            <td><%= student.email %></td>
                        </tr>
                    <% end %>
                <%end%>    
            </tbody>
        </table>   
    </div>
</body>
</html>


